# Claude response — Codex review round 2

Date: 2026-07-19. Refers to `CODEX_REVIEW_ROUND_2.md` (resolution check + S1–S12) and produces `PROJECT_PLAN.md` v1.2.

Tally: **12 of 12 new findings accepted** (S3 accepted with a specific policy choice). All partially-resolved round-1 items (R2, R9, R10, R12, R13, R15, R20) are closed by the corresponding S-fixes below. Codex endorsed my round-1 rationales for R2, R8, R13, R17 and R18 and escalated R15 → handled at S3.

| Finding | Severity | Verdict | Response (change in v1.2) |
|---------|----------|---------|---------------------------|
| S1 decimal comma vs comma splitting | Major | **Accepted** | §8.4 now defines tokenization precedence: step 0 rewrites a comma *directly between two digits* (`22,5` → `22.5`) before any segment splitting; remaining commas split segments. Documented limitation: `10,1x8` with no space is read as a decimal — a comma-space (`10, 1x8`) is required between segments that start with a digit, which matches both typing and iOS dictation output. Fixtures added mixing decimal commas with multi-segment input. |
| S2 import validates before migrating | Major | **Accepted** | §16 validation order corrected: (1) envelope + size/count caps, (2) coarse structural check (arrays of objects), (3) apply migration record-functions from the file's version, (4) **full current-schema validation — types, ranges, uniqueness, foreign keys — on the migrated result**, (5) settings normalisation, (6) preview → safety copy → transactional replace. Old backups can no longer be rejected for missing fields a later migration supplies. |
| S3 malformed records exported but unimportable | Major | **Accepted (policy chosen)** | Coherent policy: exports place schema-valid records in the normal arrays and malformed raw records in a separate top-level `unreadable` array. Import restores the valid arrays and **never inserts `unreadable` entries**; when the array is non-empty the preview states "N unreadable entries stay in the backup file for manual inspection and won't be restored". Good data is always recoverable; corrupt blobs are preserved in the file, not resurrected into the DB. Corrupt-export round-trip test added (§17.1). |
| S4 historical-edit timezone undefined | Major | **Accepted** | §11.1: a timestamp edit interprets the chosen wall-clock time in the phone's **current** timezone rules for that date (JS `Date` semantics; DST gaps roll forward, folds resolve to the first occurrence) and re-derives `tzOffsetMin` accordingly; an edit that doesn't touch the timestamp leaves `performedAtMs`/`tzOffsetMin` unchanged. The edit sheet already shows the resulting workout day, so travel/DST day-shifts are visible before saving. |
| S5 version-skew guarantee overstated | Major | **Accepted** | §10 adds an enforced connection protocol: every open DB registers `onversionchange` → close + full-screen "App updated — reload" overlay; `open()` `onblocked` → plain-language "close other tabs of this app" view with retry. §14 rewords the guarantee as the outcome of this protocol, not an assumption. Two-tab upgrade added as browser scenario B7. |
| S6 browser tests unenumerated, late, out of CI | Major | **Accepted** | §17.0 enumerates the minimum suite B1–B7 (load/navigate; add exercise; stepper log + reload persistence; quick-entry sentence; edit/delete/undo; offline reload; two-tab versionchange). §18 wires Playwright in Phase 0 (B1) and grows the suite in the phase that builds each behaviour. CI runs Vitest + `check:precache` + the Playwright suite (§24). |
| S7 no path for an unopenable DB | Major | **Accepted** | §12 now has a recovery matrix: transient open failure → Retry; blocked → S5 view; failed migration → atomic abort, retry next launch, restore guidance; **repeatedly unopenable/corrupt → guided "Reset app storage"** (`indexedDB.deleteDatabase` + recreate + import), gated behind a typed confirmation that states all local data will be erased and first prompts the owner to locate a backup file. Injected-failure tests for each reachable state (§17.1). |
| S8 FK check not transactional | Minor | **Accepted** | §8.2: exercise existence is verified *inside the same readwrite transaction* (spanning `exercises` + `sets`) that writes a set; abort if missing. Two-store-instance race test added. |
| S9 acceptance gaps | Minor | **Accepted** | Added: §19.3 post-Undo-expiry permanence; §19.5 failed/declined safety copy requires the explicit extra confirmation (verified); §19.6 update toast → reload lands on the new version (device); §17.2 gains concrete a11y gates (VoiceOver walkthrough of the log flow, 200 % zoom, contrast spot-check). Each tagged unit/browser/device. |
| S10 undefined v1 versions, placeholder migrations | Minor | **Accepted** | §10 declares `DB_VERSION = 1`, empty migrations table in v1, supported import versions "1 … DB_VERSION"; the misleading `schemaVersion: 3` example in §16 corrected to 1; "no-op chain test" dropped in favour of bootstrap + newer-refusal tests now, and a MAINTENANCE.md rule that the first real migration must add fixture tests. No invented history. |
| S11 Home summary ambiguous | Minor | **Accepted** | §6.1 fixes the format: weight-mode "Tue · 3 sets · top 10 kg"; reps-mode "Tue · 3 sets · best 12 reps". No per-set aggregation guessing. |
| S12 Phase 3 gate references device numbers | Minor | **Accepted** | §18 Phase 3 exit is explicitly functional-in-browser; the numeric tap bounds are measured only at the Phase 9 device gate. |

## On Codex's recommendation of a targeted third round

Codex's final verdict asks for a short verification pass because S1–S7 sit on the highest-risk boundaries (migration, restore, corruption recovery, SW/IDB coordination). The agreed process (REQUIREMENTS §6) mandates *at least* two rounds; a capped, verification-only third pass is cheap, automated, and reduces data-loss risk, so the lead developer accepts it: **one targeted round confirming S1–S12 landed correctly, then the plan is finalised regardless**, with anything still open surfaced to the owner at the checkpoint. No further rounds.
